#include <stdio.h>

int main(){
    int vetor[10];
    int i;

    printf ("Digite 10 numeros inteiros:\n");
    scanf ("%d" &vetor[i]);
}

printf ("\nValores na ordem em que foram digitados:\n");
for (i = 0; i < 10, i++){
    printf ("%d", vetor[i]);
}

printf ("\nValores na ordem inversa:\n");
for (i=9; i >= 0; i--){
    printf ("%d", vetor[i]);
}

printf ("\n");

return 0;
